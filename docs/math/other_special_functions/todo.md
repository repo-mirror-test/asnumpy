## Other Special Functions （特殊函数，TODO）

**计划支持的 API 数量：1**  
**未来计划支持的 API：** [i0](#i0)

> 说明：本节为特殊函数的预留接口，目前仅规划 `i0`，后续可能补充更多常见特殊函数。

---

<span id="i0">1. <mark> **i0** </mark></span>

- **计划参数：**  
  x：输入数组或标量（浮点）；  
  out：可选，输出数组。

- **计划返回类型：**  
  NPUArray（与 x 广播后形状一致）

- **计划支持的 dtype：**  
  各类浮点类型，后续视需求扩展。

- **计划功能描述：**  
  逐元素计算修正贝塞尔函数 I₀(x)（第一类 0 阶）。